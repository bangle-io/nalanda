import { CodeBlockVanilla } from '../../components/CodeBlock';

## Usage

The simplest way to use nalanda is to setup a store, slice and a state.

```typescript
const key = createKey('mySliceName', dependencies);

const counter = key.state(0);

function increment() {
  return counter.update((prev) => prev + 1);
}

const counterSlice = key.slice({
  counter,
});

const store = createStore({
  slices: [slice],
});

console.log(counterSlice.get(store.state)); // 0

store.dispatch(increment());

console.log(counterSlice.get(store.state)); // 1
```

<CodeBlockVanilla>
{`
import { createKey, createStore } from "nalanda";

const key = createKey("mySliceName");

const counter = key.state(0);

const counterSlice = key.slice({
  counter,
});

const store = createStore({
  slices: [counterSlice],
});

console.log(counter.get(store.state));

console.log(counterSlice.get(store.state));

`}

</CodeBlockVanilla>
